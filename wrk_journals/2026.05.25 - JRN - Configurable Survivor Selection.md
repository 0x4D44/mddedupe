# JRN — Configurable Survivor Selection (implementation)

- **Started:** 2026-05-25
- **Branch:** `feature/configurable-survivor-selection`
- **Design:** `wrk_docs/2026.05.24 - HLD - Configurable Survivor Selection.md` (rev. 2)
- **Role:** Claude = supervisor (high-level direction only; no hands-on test/debug).
  Implementation, review, and fixes delegated to agents.

## Team model

- **Implementer** (opus) — writes code TDD-first, keeps `cargo build/test/clippy/fmt` green.
- **Reviewer** (code-reviewer skill) — reviews each stage's diff, writes a CR doc to `wrk_docs/`.
- **Devil's advocate** (opus) — independently challenges the work: hunts for the
  weakest assumption, the untested edge, the spec gap. Runs in parallel with Reviewer.
- **Fixer** (code-review-apply-feedback skill) — applies reviewer + devil's-advocate findings.
- **Verifier** (final) — compares the finished implementation against the HLD; iterate to 100%.

## Plan (sequential stages; commit after each)

1. **Stage 1 — Selection engine (neutral default).** `FallbackStrategy`, `KeepPolicy`
   (+ neutral `Default`), `SurvivorReason`, `select_survivors`; wire into
   `process_duplicates` / read-only display / `find_duplicates_in_dirs` via
   `&KeepPolicy`, callers pass `KeepPolicy::default()`. Output + len-1 accounting
   unchanged (correct under neutral). Deps: `globset`, dev `filetime`. Existing tests
   pass `default()`; new unit tests exercise protect + fallback directly.
2. **Stage 2 — Accounting + explain + safe collapse.** Two-metric accounting
   (redundancy + removable) in `find_duplicates_in_dirs` / `process_duplicates` /
   `JsonSummary`; `KEPT (reason)` explain output both paths; protect-aware
   `collapse_group_by_identity`. Still neutral default → CLI unchanged; tested via
   explicit policies.
3. **Stage 3 — Config + CLI + flip default ON.** `toml` dep; `ConfigFile`/`KeepConfig`;
   `resolve_keep_policy`; flags `--protect-dir/--protect-name/--no-protect/--keep/--config`;
   `AppError` variants; `run_app` resolves convention default → feature ON.
4. **Stage 4 — Docs.** `ARCHITECTURE.md`, `CLAUDE.md`, `docs/usage.md`, `readme.md`.
5. **Final verification.** Agent team diffs implementation vs HLD; iterate to 100%.

## Status log

- 2026-05-25 — Branch created; journal + design committed. Launching Stage 1 implementer.
